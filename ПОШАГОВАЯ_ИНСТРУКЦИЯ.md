# 📖 ПОШАГОВАЯ ИНСТРУКЦИЯ для начинающих

## 🎯 Цель: Запустить систему аукционов на вашем компьютере

Следуйте инструкциям **СТРОГО ПО ПОРЯДКУ**. Не пропускайте шаги!

---

## ШАГ 1: Установка Node.js ⏱ 5 минут

### Что делать:

1. Откройте браузер и перейдите: https://nodejs.org/
2. Скачайте **Node.js 20.x LTS** (зелёная кнопка)
3. Запустите установщик и нажимайте "Next" (все настройки по умолчанию)
4. После установки перезапустите компьютер (или хотя бы закройте и откройте терминал заново)

### Как проверить:

Откройте новый терминал (PowerShell или CMD) и выполните:
```bash
node --version
npm --version
```

Должны показаться версии, например:
```
v20.10.0
10.2.3
```

✅ **Если видите версии - переходите к следующему шагу.**

❌ **Если ошибка "не является внутренней или внешней командой"** - Node.js не установлен или не добавлен в PATH. Переустановите Node.js и при установке отметьте галочку "Add to PATH".

---

## ШАГ 2: Установка Docker Desktop ⏱ 10 минут

### Что делать:

1. Откройте: https://www.docker.com/products/docker-desktop/
2. Скачайте **Docker Desktop for Windows**
3. Установите (может попросить включить WSL 2 - включите)
4. После установки **запустите Docker Desktop** (иконка появится в трее Windows)

### Как проверить:

В терминале выполните:
```bash
docker --version
```

Должно показаться что-то вроде:
```
Docker version 24.0.7
```

✅ **Если видите версию - переходите к следующему шагу.**

❌ **Если ошибка** - Docker Desktop не запущен. Откройте Docker Desktop из меню Пуск и дождитесь, пока иконка в трее станет зелёной.

---

## ШАГ 3: Создание файла .env ⏱ 2 минуты

### Что делать:

**Вариант A - Автоматически (рекомендуется):**

Дважды кликните на файл `create-env.bat` в папке проекта. Он создаст `.env` автоматически.

**Вариант B - Вручную:**

1. Откройте папку `C:\Konkurs` в проводнике Windows
2. Правой кнопкой → Создать → Текстовый документ
3. Назовите файл: `.env` (обязательно с точкой в начале!)
   - Если Windows не даёт назвать с точкой, назовите `env.txt`
   - Затем в терминале: `ren env.txt .env`
4. Откройте файл блокнотом и вставьте:

```
NODE_ENV=development
PORT=3000
MONGODB_URI=mongodb://admin:password123@localhost:27017/auctions?authSource=admin&replicaSet=rs0
REDIS_URL=redis://localhost:6379
JWT_SECRET=your-secret-key-change-in-production-change-this-in-real-app
```

5. Сохраните файл (Ctrl+S)

### Как проверить:

В терминале (из папки `C:\Konkurs`):
```bash
type .env
```

Должен показать содержимое файла.

✅ **Если видите содержимое - переходите к следующему шагу.**

---

## ШАГ 4: Установка зависимостей проекта ⏱ 5 минут

### Что делать:

1. Откройте терминал (PowerShell или CMD)
2. Перейдите в папку проекта:
   ```bash
   cd C:\Konkurs
   ```

3. Установите зависимости:
   ```bash
   npm install
   ```

   ⚠️ **Это займёт 2-5 минут!** Будет много текста, это нормально.

### Как проверить:

После завершения выполните:
```bash
dir node_modules
```

Должна быть папка `node_modules` с множеством подпапок.

✅ **Если папка существует - переходите к следующему шагу.**

❌ **Если были ошибки:**
- Проверьте интернет-соединение
- Попробуйте: `npm cache clean --force` затем `npm install` снова
- Убедитесь, что Node.js установлен правильно

---

## ШАГ 5: Установка зависимостей frontend ⏱ 3 минуты

### Что делать:

В том же терминале:
```bash
cd frontend
npm install
cd ..
```

### Как проверить:

```bash
dir frontend\node_modules
```

Должна быть папка с зависимостями.

✅ **Если папка существует - переходите к следующему шагу.**

---

## ШАГ 6: Запуск MongoDB и Redis ⏱ 2 минуты

### Что делать:

**ВАЖНО:** Docker Desktop должен быть запущен! (проверьте иконку в трее)

В терминале (из папки `C:\Konkurs`):
```bash
docker-compose up -d mongodb redis mongo-setup
```

Подождите 15-20 секунд.

### Как проверить:

```bash
docker-compose ps
```

Должны быть контейнеры:
- `auction-mongodb` - статус "Up"
- `auction-redis` - статус "Up"
- `auction-mongo-setup` - может быть "Exited" (это нормально)

✅ **Если контейнеры "Up" - переходите к следующему шагу.**

❌ **Если ошибка "Cannot connect to Docker":**
- Убедитесь, что Docker Desktop запущен
- Попробуйте запустить Docker Desktop из меню Пуск

---

## ШАГ 7: Сборка проекта ⏱ 1 минута

### Что делать:

```bash
npm run build
```

Будет компилировать TypeScript в JavaScript.

### Как проверить:

```bash
dir dist
```

Должна быть папка `dist` с файлами `.js`.

✅ **Если папка существует - переходите к следующему шагу.**

❌ **Если были ошибки TypeScript:**
- Это могут быть просто предупреждения (warnings) - это нормально
- Если есть ошибки (errors) - посмотрите, что они говорят
- Попробуйте: `npm install` снова, затем `npm run build`

---

## ШАГ 8: Создание тестовых данных ⏱ 30 секунд

### Что делать:

```bash
npm run create-test-data
```

Создаст тестовых пользователей и аукционы.

### Как проверить:

В выводе должно быть что-то вроде:
```
✅ Test data created successfully!
Active Auction ID: 507f1f77bcf86cd799439011
```

⚠️ **ЗАПОМНИТЕ ID АУКЦИОНА!** Он понадобится позже.

✅ **Если видите успешное сообщение - переходите к следующему шагу.**

---

## ШАГ 9: Запуск backend сервера ⏱ 30 секунд

### Что делать:

```bash
npm run dev
```

Должно появиться:
```
✅ MongoDB connected successfully
✅ Redis connected
🚀 Server running on port 3000
```

**⚠️ НЕ ЗАКРЫВАЙТЕ ЭТОТ ТЕРМИНАЛ!** Оставьте его работать.

### Как проверить:

Откройте браузер и перейдите: http://localhost:3000/health

Должен вернуться JSON:
```json
{"status":"ok","timestamp":"2024-..."}
```

✅ **Если видите JSON - сервер работает!**

❌ **Если ошибка подключения к MongoDB:**
- Подождите ещё 10 секунд и попробуйте снова
- Проверьте: `docker-compose ps` - контейнеры должны быть "Up"
- Проверьте логи: `docker-compose logs mongodb`

---

## ШАГ 10: Запуск frontend ⏱ 30 секунд

### Что делать:

**Откройте НОВЫЙ терминал** (не закрывая первый с backend):

```bash
cd C:\Konkurs\frontend
npm run dev
```

Должно появиться:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
```

### Как проверить:

Откройте браузер: **http://localhost:5173**

Должен открыться интерфейс аукционов!

✅ **Если видите интерфейс - ВСЁ РАБОТАЕТ!**

---

## 🎉 ГОТОВО! Что делать дальше?

### Тестирование через веб-интерфейс:

1. Откройте http://localhost:5173
2. Кликните на аукцион в списке
3. Нажмите "Balance" в меню
4. Пополните баланс (например, 50000)
5. Вернитесь к аукциону
6. Сделайте ставку (например, 150)

### Тестирование через API:

```bash
# Получить список аукционов
curl http://localhost:3000/api/auctions
```

### Запуск ботов:

В новом терминале:
```bash
cd C:\Konkurs
npm run bot <ID_АУКЦИОНА> 5
```

Где `<ID_АУКЦИОНА>` - это ID из шага 8.

---

## ❌ Что делать, если что-то не работает?

### Быстрые решения:

1. **Ошибки импорта TypeScript?**
   → Выполните: `npm install` (см. `FIX_ERRORS.md`)

2. **MongoDB не подключается?**
   → Подождите 20 секунд после запуска Docker
   → Проверьте: `docker-compose ps`

3. **Порт занят?**
   → Измените `PORT=3001` в файле `.env`

4. **Всё ещё не работает?**
   → Прочитайте `SETUP_GUIDE_RU.md` - там подробнее
   → Или `QUICK_FIX.md` - там быстрые решения

---

## 📚 Полезные файлы

- `START_HERE.md` - краткая инструкция
- `SETUP_GUIDE_RU.md` - подробное руководство
- `QUICK_FIX.md` - быстрые решения проблем
- `CREATE_ENV.md` - как создать .env
- `FIX_ERRORS.md` - как исправить ошибки
- `README.md` - полное описание системы

---

## ✅ Чеклист финальной проверки

Проверьте, что у вас:

- [ ] Node.js установлен (`node --version` работает)
- [ ] Docker Desktop установлен и запущен
- [ ] Файл `.env` существует в `C:\Konkurs`
- [ ] Зависимости установлены (`npm install` выполнен)
- [ ] Контейнеры Docker запущены (`docker-compose ps`)
- [ ] Проект собран (`npm run build`)
- [ ] Backend запущен (`npm run dev`)
- [ ] Frontend запущен (`npm run dev` в папке frontend)
- [ ] Открывается http://localhost:5173

**Если все пункты отмечены - система работает! 🎉**

---

**Удачи! Если возникнут проблемы - читайте документацию!** 🚀

