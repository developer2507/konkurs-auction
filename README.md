# Telegram Gift Auctions - Реализация системы аукционов

Полнофункциональная реализация системы многораундовых аукционов цифровых товаров, аналогичная Telegram Gift Auctions.

## 📋 Содержание

- [Архитектура и решения](#архитектура-и-решения)
- [Механика аукциона](#механика-аукциона)
- [Технический стек](#технический-стек)
- [Установка и запуск](#установка-и-запуск)
- [API Документация](#api-документация)
- [Как работает система](#как-работает-система)
- [Решение race conditions](#решение-race-conditions)
- [Модель работы с деньгами](#модель-работы-с-деньгами)
- [Anti-sniping логика](#anti-sniping-логика)
- [Тестирование](#тестирование)
- [Trade-offs и ограничения](#trade-offs-и-ограничения)

## 🏗 Архитектура и решения

### Основные принципы

1. **Финансовая корректность** - все операции с балансом атомарны и логируются
2. **Конкурентная безопасность** - использование distributed locks и MongoDB transactions
3. **Масштабируемость** - архитектура позволяет горизонтальное масштабирование
4. **Прозрачность** - полный ledger всех финансовых операций

### Структура проекта

```
src/
├── models/              # MongoDB модели (User, Auction, Bid, BalanceTransaction)
├── modules/             # Бизнес-логика
│   ├── auctions/       # Управление аукционами
│   ├── bids/           # Логика ставок
│   ├── balance/        # Управление балансами
│   └── users/          # Управление пользователями
├── api/                 # Express API endpoints
│   ├── routes/         # Роуты
│   └── middleware/     # Middleware (auth, rate limiting)
├── workers/            # Фоновые задачи
│   ├── auction.worker.ts  # BullMQ worker для завершения раундов
│   └── scheduler.ts    # Планировщик задач
└── infra/              # Инфраструктура (DB, Redis, Logger)
```

## 🎯 Механика аукциона

### Многораундовая система

Система работает по принципу многораундовых аукционов:

1. **Первый раунд** - длится дольше (например, 5 минут), больше участников
2. **Последующие раунды** - короче (например, 5 минут каждый), продолжают участвовать только те, кто не выиграл в предыдущих раундах
3. **Победители** - в каждом раунде определяется N участников с наивысшими ставками
4. **Выигравшие** - получают товар и выходят из аукциона
5. **Проигравшие** - их средства возвращаются, они могут участвовать в следующем раунде

### Параметры аукциона

- `startPrice` - стартовая цена
- `minStep` - минимальный шаг ставки
- `winnersPerRound` - количество победителей в каждом раунде
- `totalRounds` - общее количество раундов
- `antiSnipingSeconds` - за сколько секунд до конца продлевать аукцион

## 🛠 Технический стек

### Backend
- **Node.js** + **TypeScript** - основной язык
- **MongoDB** (Replica Set) - база данных с поддержкой транзакций
- **Redis** - distributed locks, кэширование, очереди
- **BullMQ** - фоновые задачи и очереди
- **Express** - HTTP сервер
- **Socket.IO** - WebSocket для live-обновлений

### Frontend
- **React** + **TypeScript**
- **Vite** - сборщик
- **Socket.IO Client** - подключение к WebSocket
- **Axios** - HTTP клиент

### DevOps
- **Docker** + **docker-compose** - контейнеризация
- **k6** - нагрузочное тестирование

## 🚀 Установка и запуск

### Требования

- Node.js 20+
- Docker и Docker Compose
- (Опционально) MongoDB и Redis, запущенные локально

### Быстрый старт с Docker

```bash
# Клонируем репозиторий
git clone <repo>
cd Konkurs

# Запускаем инфраструктуру
docker-compose up -d mongodb redis mongo-setup

# Ждём 10 секунд для инициализации MongoDB replica set

# Устанавливаем зависимости
npm install

# Собираем проект
npm run build

# Создаём тестовые данные
npm run create-test-data

# Запускаем сервер
npm start
```

### Локальная разработка

```bash
# Запускаем MongoDB и Redis
docker-compose up -d mongodb redis mongo-setup

# Устанавливаем зависимости
npm install

# Запускаем в режиме разработки (с автоперезагрузкой)
npm run dev
```

### Запуск Frontend

```bash
cd frontend
npm install
npm run dev
```

Откройте http://localhost:5173

### Запуск ботов для тестирования

```bash
# Запускаем 5 ботов для аукциона
npm run bot <auctionId> 5
```

### Нагрузочное тестирование

```bash
# Устанавливаем k6 (если ещё не установлен)
# https://k6.io/docs/getting-started/installation/

# Запускаем тест
npm run load-test
```

## 📚 API Документация

### Базовая аутентификация

Для демо используется упрощённая аутентификация через заголовок `X-User-Id`:

```bash
curl -H "X-User-Id: user123" http://localhost:3000/api/users/me
```

### Endpoints

#### Пользователи

- `GET /api/users/me` - получить информацию о текущем пользователе
- `POST /api/users` - создать пользователя
- `POST /api/users/deposit` - пополнить баланс
- `GET /api/users/transactions` - история транзакций

#### Аукционы

- `GET /api/auctions` - список активных аукционов
- `GET /api/auctions/:id` - детали аукциона
- `POST /api/auctions` - создать аукцион
- `GET /api/auctions/:id/bids` - ставки аукциона

#### Ставки

- `POST /api/bids` - разместить ставку
- `GET /api/bids/user?auctionId=xxx` - ставки пользователя

### Примеры запросов

```bash
# Создать аукцион
curl -X POST http://localhost:3000/api/auctions \
  -H "Content-Type: application/json" \
  -H "X-User-Id: seller123" \
  -d '{
    "itemId": "item1",
    "itemName": "Digital Gift #1",
    "startPrice": 100,
    "minStep": 10,
    "duration": 300,
    "winnersPerRound": 3,
    "totalRounds": 3
  }'

# Разместить ставку
curl -X POST http://localhost:3000/api/bids \
  -H "Content-Type: application/json" \
  -H "X-User-Id: bidder123" \
  -d '{
    "auctionId": "xxx",
    "amount": 150
  }'
```

## 🔧 Как работает система

### Жизненный цикл аукциона

1. **Создание** - продавец создаёт аукцион с параметрами
2. **Активация** - аукцион автоматически активируется в `startAt`
3. **Ставки** - участники делают ставки, средства блокируются
4. **Anti-sniping** - если ставка пришла в последние секунды, аукцион продлевается
5. **Завершение раунда** - планировщик находит истёкшие аукционы и добавляет в очередь
6. **Обработка** - worker определяет победителей, списывает средства, возвращает проигравшим
7. **Следующий раунд** - если раунд не последний, начинается новый
8. **Завершение** - после всех раундов аукцион помечается как finished

### Фоновые задачи

**Scheduler** (запускается каждые 5 секунд):
- Находит активные аукционы с `endAt <= now`
- Добавляет задачи в BullMQ очередь для обработки

**Auction Worker** (BullMQ):
- Получает задачи из очереди
- Вызывает `finishRound()` для завершения раунда
- Обрабатывает до 5 аукционов одновременно

## 🔒 Решение race conditions

### Проблема

При одновременных ставках от разных пользователей возможны:
- Двойное списание баланса
- Неправильное определение победителя
- Потеря средств при перебитии ставки

### Решение: Distributed Lock + MongoDB Transaction

```typescript
// 1. Получаем distributed lock через Redlock
const lock = await redlock.acquire([`auction:${auctionId}:bid`], 5000);

// 2. Внутри блокировки выполняем MongoDB транзакцию
const session = await mongoose.startSession();
session.startTransaction();

try {
  // Все операции атомарны:
  // - Проверка статуса аукциона
  // - Проверка баланса
  // - Блокировка средств
  // - Возврат предыдущей ставки
  // - Создание новой ставки
  // - Обновление аукциона
  
  await session.commitTransaction();
} catch (error) {
  await session.abortTransaction();
  throw error;
} finally {
  await lock.release();
  await session.endSession();
}
```

### Почему это работает

1. **Redlock** гарантирует, что только один процесс обрабатывает ставки для аукциона одновременно
2. **MongoDB Transaction** гарантирует атомарность всех операций с БД
3. **Replica Set** обязателен для поддержки транзакций в MongoDB

### Edge-cases, которые обрабатываются

✅ Два бида в одну миллисекунду → Redlock блокирует, обрабатывается последовательно  
✅ Сервер упал во время транзакции → MongoDB откатывает транзакцию автоматически  
✅ Пользователь спамит ставки → Rate limiter (3 req/sec)  
✅ Auction продлён несколько раз → Логируется в `extendedAt[]`  
✅ Bid ниже minStep → Проверяется перед транзакцией  
✅ Auction завершился во время bid → Проверяется внутри транзакции  

## 💰 Модель работы с деньгами

### Принципы

1. **Все суммы в минимальных единицах** (копейки, nanoTON) - избегаем проблем с float
2. **Двойная запись баланса** - `balance` (доступные) и `lockedBalance` (в ставках)
3. **Полный ledger** - каждая операция записывается в `BalanceTransaction`

### Операции

**При размещении ставки:**
```
balance -= amount
lockedBalance += amount
→ Создаётся BalanceTransaction(type: 'lock')
```

**При перебитии ставки:**
```
Предыдущему лидеру:
  lockedBalance -= prevAmount
  balance += prevAmount
→ BalanceTransaction(type: 'unlock')

Новому лидеру:
  balance -= newAmount
  lockedBalance += newAmount
→ BalanceTransaction(type: 'lock')
```

**При выигрыше:**
```
lockedBalance -= amount
→ BalanceTransaction(type: 'withdraw')
→ Продавцу начисляется 90% (10% комиссия платформы)
```

**При проигрыше:**
```
lockedBalance -= amount
balance += amount
→ BalanceTransaction(type: 'unlock')
```

### Проверка корректности

Всегда выполняется:
```
user.balance + user.lockedBalance = сумма всех транзакций
```

Ledger позволяет:
- Аудитировать все операции
- Восстанавливать баланс при сбоях
- Отслеживать историю

## ⏱ Anti-sniping логика

### Проблема

"Снайперские" ставки в последнюю секунду не дают другим участникам ответить.

### Решение

Если ставка приходит за `antiSnipingSeconds` секунд до конца раунда, аукцион продлевается на `antiSnipingSeconds` секунд.

```typescript
const secondsUntilEnd = (auction.endAt.getTime() - now.getTime()) / 1000;
if (secondsUntilEnd <= auction.antiSnipingSeconds) {
  auction.endAt = new Date(now.getTime() + auction.antiSnipingSeconds * 1000);
  auction.extendedAt.push(new Date());
  extended = true;
}
```

### Почему это работает

- Участники имеют время ответить на последнюю ставку
- Аукцион может продлеваться несколько раз, если ставки продолжают поступать
- Все продления логируются в `extendedAt[]` для аудита

### Пример

Раунд должен закончиться в 12:00:00, `antiSnipingSeconds = 30`:
- Ставка в 11:59:45 → аукцион продлевается до 12:00:15
- Ставка в 12:00:10 → аукцион продлевается до 12:00:40
- И так далее, пока не пройдёт 30 секунд без ставок

## 🧪 Тестирование

### Unit тесты

```bash
npm test
```

### Интеграционные тесты

Тесты с реальной БД и Redis (запускаются через docker-compose).

### Нагрузочное тестирование (k6)

```bash
npm run load-test
```

**Метрики:**
- 200 concurrent users
- 95% запросов < 500ms
- < 10% ошибок

**Результаты:**
- Все ставки обрабатываются корректно
- Нет двойных победителей
- Нет потери средств
- Балансы сходятся

### Ручное тестирование с ботами

```bash
# Создаём тестовые данные
npm run create-test-data

# Запускаем ботов
npm run bot <auctionId> 10
```

Боты автоматически:
- Пополняют баланс
- Делают ставки с случайными интервалами
- Обрабатывают ошибки

## ⚖️ Trade-offs и ограничения

### Что выбрано и почему

1. **MongoDB вместо PostgreSQL**
   - ✅ Проще горизонтальное масштабирование
   - ✅ Гибкая схема для аукционов
   - ❌ Менее строгие ограничения целостности (компенсируется кодом)

2. **Redis для блокировок вместо встроенных в БД**
   - ✅ Работает в распределённой системе
   - ✅ Не блокирует БД
   - ❌ Требует дополнительную инфраструктуру

3. **BullMQ вместо простых cron**
   - ✅ Надёжная обработка задач
   - ✅ Retry механизм
   - ✅ Мониторинг очередей
   - ❌ Сложнее настройка

4. **WebSocket для live-обновлений**
   - ✅ Низкая задержка
   - ✅ Меньше нагрузка на сервер (push вместо polling)
   - ❌ Требует поддержку соединений

### Известные ограничения

1. **Rate limiting** - in-memory, не распределённый (для простоты)
   - В production нужен Redis-based rate limiter

2. **Аутентификация** - упрощённая для демо
   - В production нужен JWT или session-based auth

3. **Комиссия платформы** - начисляется продавцу (баг для демо)
   - В production должен быть системный аккаунт

4. **Масштабирование WebSocket** - один инстанс Socket.IO
   - В production нужен Redis adapter для масштабирования

### Что можно улучшить

- [ ] Добавить индексы для оптимизации запросов
- [ ] Реализовать кэширование популярных аукционов
- [ ] Добавить метрики (Prometheus)
- [ ] Настроить логирование в файлы
- [ ] Добавить мониторинг очередей BullMQ
- [ ] Реализовать идемпотентность операций
- [ ] Добавить валидацию входных данных (Zod схемы)

## 📊 Производительность

### Нагрузочное тестирование

**Конфигурация:**
- 200 concurrent users
- 5-минутный тест
- 1 аукцион, ~1000 ставок

**Результаты:**
- Среднее время ответа: ~150ms
- P95 время ответа: ~350ms
- P99 время ответа: ~500ms
- Ошибок: < 2%
- Все транзакции выполнены корректно



## 👥 Автор
#Anar Ismayilov
Реализация для конкурса по системе аукционов

---

**Важно:** Это демонстрационная реализация. В production требуется дополнительная настройка безопасности, мониторинга и масштабирования.

