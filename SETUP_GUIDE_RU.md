# 🚀 Полное руководство по установке и настройке

## Шаг 1: Установка необходимых программ

### 1.1 Установка Node.js

1. Скачайте Node.js с официального сайта: https://nodejs.org/
2. Рекомендуемая версия: **Node.js 20.x LTS**
3. Установите, следуя инструкциям установщика
4. Проверьте установку:
   ```bash
   node --version
   npm --version
   ```
   Должны быть версии v20.x.x и 10.x.x соответственно

### 1.2 Установка Docker Desktop

1. Скачайте Docker Desktop: https://www.docker.com/products/docker-desktop/
2. Установите и запустите Docker Desktop
3. Убедитесь, что Docker запущен (иконка в трее должна быть зелёной)
4. Проверьте установку:
   ```bash
   docker --version
   docker-compose --version
   ```

### 1.3 (Опционально) Установка MongoDB отдельно

Если вы хотите использовать MongoDB без Docker:

1. Скачайте MongoDB Community Server: https://www.mongodb.com/try/download/community
2. Установите MongoDB
3. Запустите MongoDB как службу

**⚠️ ВАЖНО:** Для работы транзакций нужен MongoDB Replica Set! Если устанавливаете вручную, вам нужно настроить Replica Set (это сложно). Рекомендуется использовать Docker.

## Шаг 2: Настройка проекта

### 2.1 Откройте терминал в папке проекта

```bash
cd C:\Konkurs
```

### 2.2 Установите зависимости backend

```bash
npm install
```

Это установит все необходимые пакеты. Процесс может занять 2-5 минут.

Если видите ошибки:
- Убедитесь, что Node.js установлен
- Попробуйте удалить `node_modules` и `package-lock.json`, затем `npm install` снова
- Если ошибки с правами доступа, запустите терминал от имени администратора

### 2.3 Проверьте файл .env

Файл `.env` должен быть в корне проекта. Если его нет:

1. Создайте файл `.env` в папке `C:\Konkurs`
2. Скопируйте туда следующее содержимое:

```env
NODE_ENV=development
PORT=3000
MONGODB_URI=mongodb://admin:password123@localhost:27017/auctions?authSource=admin&replicaSet=rs0
REDIS_URL=redis://localhost:6379
JWT_SECRET=your-secret-key-change-in-production-change-this-in-real-app
```

**⚠️ Если у вас своя установка MongoDB, измените `MONGODB_URI` соответственно.**

### 2.4 Установите зависимости frontend

```bash
cd frontend
npm install
cd ..
```

## Шаг 3: Запуск MongoDB и Redis через Docker

### 3.1 Запустите Docker Desktop

Убедитесь, что Docker Desktop запущен (иконка в трее Windows).

### 3.2 Запустите контейнеры

В терминале (из папки `C:\Konkurs`):

```bash
docker-compose up -d mongodb redis mongo-setup
```

Эта команда:
- Запустит MongoDB в контейнере
- Запустит Redis в контейнере
- Настроит MongoDB Replica Set (нужен для транзакций)

### 3.3 Подождите 15-20 секунд

MongoDB нужно время для инициализации Replica Set.

### 3.4 Проверьте, что контейнеры запущены

```bash
docker-compose ps
```

Вы должны увидеть 3 контейнера в статусе "Up":
- `auction-mongodb`
- `auction-redis`
- `auction-mongo-setup` (может быть в статусе "Exited" - это нормально, он выполнил свою работу)

### 3.5 Если MongoDB не запустился

Проверьте логи:
```bash
docker-compose logs mongodb
```

Если видите ошибки с Replica Set, выполните вручную:

```bash
docker exec -it auction-mongodb mongosh -u admin -p password123 --authenticationDatabase admin
```

Затем в MongoDB shell выполните:
```javascript
rs.initiate({
  _id: 'rs0',
  members: [{ _id: 0, host: 'localhost:27017' }]
});
```

Нажмите `exit` для выхода.

## Шаг 4: Проверка подключения к базе данных

### 4.1 Проверка MongoDB

```bash
docker exec -it auction-mongodb mongosh -u admin -p password123 --authenticationDatabase admin
```

Если подключение успешно, вы увидите MongoDB shell. Выполните:
```javascript
rs.status()
```

Если видите информацию о Replica Set - всё хорошо. Выйдите командой `exit`.

### 4.2 Проверка Redis

```bash
docker exec -it auction-redis redis-cli ping
```

Должен ответить: `PONG`

## Шаг 5: Сборка проекта

### 5.1 Соберите TypeScript код

```bash
npm run build
```

Это скомпилирует TypeScript код в JavaScript в папку `dist/`.

**Если видите ошибки:**
- Убедитесь, что все зависимости установлены: `npm install`
- Проверьте, что Node.js версии 20+
- Попробуйте удалить папку `dist` и собрать заново

## Шаг 6: Создание тестовых данных

```bash
npm run create-test-data
```

Эта команда создаст:
- 10 тестовых пользователей
- 2 тестовых аукциона (1 активный, 1 запланированный)

**⚠️ ВАЖНО:** Запомните ID активного аукциона из вывода! Он понадобится для тестирования.

## Шаг 7: Запуск сервера

### 7.1 Запустите backend

```bash
npm run dev
```

Или для production:
```bash
npm start
```

Вы должны увидеть:
```
✅ MongoDB connected successfully
✅ Redis connected
🚀 Server running on port 3000
```

**Если видите ошибки:**

**Ошибка "MongoDB connection error":**
- Убедитесь, что Docker контейнеры запущены: `docker-compose ps`
- Проверьте, что MongoDB инициализирован (подождите ещё 10 секунд)
- Проверьте строку подключения в `.env`

**Ошибка "Cannot find module":**
- Убедитесь, что выполнили `npm install`
- Удалите `node_modules` и `package-lock.json`, затем `npm install` снова

**Ошибка TypeScript:**
- Выполните `npm run build` сначала
- Проверьте версию Node.js: `node --version` (должна быть 20+)

### 7.2 Проверьте работу API

Откройте в браузере или выполните в терминале:

```bash
curl http://localhost:3000/health
```

Или просто откройте в браузере: http://localhost:3000/health

Должен вернуться JSON: `{"status":"ok","timestamp":"..."}`

## Шаг 8: Запуск frontend

### 8.1 Откройте новый терминал

Оставьте backend запущенным в первом терминале, во втором терминале:

```bash
cd C:\Konkurs\frontend
npm install
npm run dev
```

### 8.2 Откройте браузер

Откройте: http://localhost:5173

Вы должны увидеть интерфейс аукционов!

## Шаг 9: Тестирование системы

### 9.1 Через веб-интерфейс

1. Откройте http://localhost:5173
2. Увидите список активных аукционов
3. Кликните на аукцион
4. Нажмите "Balance" в меню
5. Пополните баланс (например, 50000)
6. Вернитесь к аукциону
7. Сделайте ставку (например, 150)

### 9.2 Через API (curl)

Создайте пользователя:
```bash
curl -X POST http://localhost:3000/api/users -H "Content-Type: application/json" -d "{\"username\":\"test\",\"initialBalance\":10000}"
```

Пополните баланс (замените USER_ID из ответа выше):
```bash
curl -X POST http://localhost:3000/api/users/deposit -H "Content-Type: application/json" -H "X-User-Id: USER_ID" -d "{\"amount\":50000}"
```

Разместите ставку (замените AUCTION_ID из npm run create-test-data):
```bash
curl -X POST http://localhost:3000/api/bids -H "Content-Type: application/json" -H "X-User-Id: USER_ID" -d "{\"auctionId\":\"AUCTION_ID\",\"amount\":150}"
```

### 9.3 Запуск ботов (автоматическое тестирование)

В новом терминале:

```bash
cd C:\Konkurs
npm run bot <AUCTION_ID> 5
```

Где `<AUCTION_ID>` - это ID аукциона из `npm run create-test-data`.

Это запустит 5 ботов, которые будут автоматически делать ставки.

## Решение проблем

### Проблема: "Cannot find module 'mongoose'"

**Решение:**
```bash
npm install
```

Если не помогло:
```bash
rm -rf node_modules package-lock.json
npm install
```

### Проблема: TypeScript ошибки при компиляции

**Решение:**
1. Убедитесь, что установлены все зависимости:
   ```bash
   npm install
   ```

2. Проверьте версию Node.js (должна быть 20+):
   ```bash
   node --version
   ```

3. Пересоберите проект:
   ```bash
   npm run build
   ```

### Проблема: MongoDB не подключается

**Решение:**
1. Проверьте, что контейнеры запущены:
   ```bash
   docker-compose ps
   ```

2. Проверьте логи MongoDB:
   ```bash
   docker-compose logs mongodb
   ```

3. Попробуйте перезапустить:
   ```bash
   docker-compose restart mongodb mongo-setup
   ```

4. Подождите 20 секунд и попробуйте снова

### Проблема: Redis не подключается

**Решение:**
```bash
docker-compose restart redis
docker-compose logs redis
```

### Проблема: Порт уже занят

**Решение:**

Измените порт в `.env`:
```env
PORT=3001
```

Или найдите и остановите процесс, занимающий порт:
```bash
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

### Проблема: Frontend не подключается к backend

**Решение:**

1. Убедитесь, что backend запущен на порту 3000
2. Проверьте файл `frontend/vite.config.ts` - там должен быть proxy:
   ```typescript
   proxy: {
     '/api': {
       target: 'http://localhost:3000',
       changeOrigin: true
     }
   }
   ```

### Проблема: Ошибки с правами доступа

**Решение:**

Запустите терминал от имени администратора:
1. Нажмите Win + X
2. Выберите "Windows PowerShell (Администратор)"
3. Перейдите в папку проекта: `cd C:\Konkurs`

## Структура базы данных MongoDB

После создания тестовых данных у вас будут следующие коллекции:

### Коллекция `users`
Хранит пользователей:
```javascript
{
  _id: ObjectId,
  tgId: Number (опционально),
  username: String,
  balance: Number, // доступные средства
  lockedBalance: Number, // заблокированные в ставках
  createdAt: Date
}
```

### Коллекция `auctions`
Хранит аукционы:
```javascript
{
  _id: ObjectId,
  itemId: String,
  itemName: String,
  sellerId: ObjectId,
  startPrice: Number,
  currentPrice: Number,
  minStep: Number,
  startAt: Date,
  endAt: Date,
  status: "scheduled" | "active" | "finished" | "cancelled",
  roundNumber: Number,
  winnersPerRound: Number,
  totalRounds: Number,
  // ...
}
```

### Коллекция `bids`
Хранит ставки:
```javascript
{
  _id: ObjectId,
  auctionId: ObjectId,
  userId: ObjectId,
  amount: Number,
  roundNumber: Number,
  isWinning: Boolean,
  createdAt: Date
}
```

### Коллекция `balancetransactions`
Хранит все финансовые операции (ledger):
```javascript
{
  _id: ObjectId,
  userId: ObjectId,
  type: "lock" | "unlock" | "withdraw" | "deposit" | "fee",
  amount: Number,
  balanceBefore: Number,
  balanceAfter: Number,
  lockedBalanceBefore: Number,
  lockedBalanceAfter: Number,
  createdAt: Date
}
```

### Просмотр данных в MongoDB

```bash
docker exec -it auction-mongodb mongosh -u admin -p password123 --authenticationDatabase admin

# Переключиться на базу данных
use auctions

# Посмотреть все коллекции
show collections

# Посмотреть пользователей
db.users.find().pretty()

# Посмотреть аукционы
db.auctions.find().pretty()

# Посмотреть ставки
db.bids.find().pretty()

# Посмотреть транзакции
db.balancetransactions.find().pretty()
```

## Что делать дальше

1. **Изучите код:**
   - Начните с `src/app.ts` - точка входа
   - Посмотрите `src/modules/` - бизнес-логика
   - Изучите `src/api/routes/` - API endpoints

2. **Тестируйте функциональность:**
   - Создавайте аукционы
   - Делайте ставки
   - Наблюдайте за завершением раундов
   - Проверяйте балансы

3. **Настройте под свои нужды:**
   - Измените параметры аукционов
   - Настройте комиссию
   - Добавьте свою логику

4. **Читайте документацию:**
   - `README.md` - полное описание системы
   - `SPEC.md` - спецификация механики
   - Комментарии в коде

## Полезные команды

```bash
# Остановить все контейнеры
docker-compose down

# Остановить и удалить данные
docker-compose down -v

# Посмотреть логи
docker-compose logs -f

# Пересобрать проект
npm run build

# Запустить тесты (когда будут добавлены)
npm test
```

---

**Если у вас остались вопросы или проблемы - проверьте логи ошибок и опишите проблему подробно!**

