# 🔧 Быстрое исправление проблем

## Проблема 1: Ошибка "Cannot find module 'mongoose'"

### Решение (30 секунд):

```bash
cd C:\Konkurs
npm install
```

Подождите 2-5 минут. После этого ошибка должна исчезнуть.

**Если не помогло:**
```bash
rmdir /s /q node_modules
del package-lock.json
npm install
```

---

## Проблема 2: Файл .env не существует

### Решение (10 секунд):

**Вариант A - Через PowerShell:**
```powershell
powershell -ExecutionPolicy Bypass -File create-env.ps1
```

**Вариант B - Через BAT файл:**
```cmd
create-env.bat
```

**Вариант C - Вручную:**
1. Создайте файл `.env` в папке `C:\Konkurs`
2. Вставьте содержимое из `CREATE_ENV.md`

---

## Проблема 3: MongoDB не подключается

### Решение:

```bash
# 1. Проверьте, что Docker запущен
docker ps

# 2. Если контейнеры не запущены, запустите их:
docker-compose up -d mongodb redis mongo-setup

# 3. Подождите 20 секунд

# 4. Проверьте логи:
docker-compose logs mongodb
```

---

## Проблема 4: TypeScript ошибки в редакторе

### Решение:

1. **Закройте и откройте редактор заново** (VS Code, WebStorm и т.д.)

2. Если не помогло:
```bash
npm install
npm run build
```

3. Если всё ещё не работает:
```bash
# Очистите кэш
npm cache clean --force
rmdir /s /q node_modules
del package-lock.json
npm install
npm run build
```

---

## Проблема 5: "Port 3000 is already in use"

### Решение:

**Вариант A - Остановить процесс:**
```bash
netstat -ano | findstr :3000
taskkill /PID <номер_из_вывода> /F
```

**Вариант B - Изменить порт:**
В файле `.env` измените:
```
PORT=3001
```

---

## Проблема 6: Frontend не подключается к backend

### Решение:

1. Убедитесь, что backend запущен:
```bash
npm run dev
```

Должно быть: `🚀 Server running on port 3000`

2. Убедитесь, что frontend запущен (в другом терминале):
```bash
cd frontend
npm run dev
```

3. Откройте в браузере: http://localhost:5173

---

## Проблема 7: Docker контейнеры не запускаются

### Решение:

```bash
# 1. Убедитесь, что Docker Desktop запущен
# (проверьте иконку в трее Windows)

# 2. Перезапустите Docker:
docker-compose down
docker-compose up -d mongodb redis mongo-setup

# 3. Если не помогло, перезапустите Docker Desktop:
# Правой кнопкой на иконке Docker → Restart
```

---

## Чеклист быстрой проверки

Проверьте эти пункты по порядку:

- [ ] Node.js установлен: `node --version` (должна быть 20+)
- [ ] Docker Desktop запущен (иконка в трее)
- [ ] Файл `.env` существует в папке `C:\Konkurs`
- [ ] Зависимости установлены: `npm install` выполнено
- [ ] Контейнеры запущены: `docker-compose ps`
- [ ] Проект собран: `npm run build` (без ошибок)

Если все пункты выполнены - всё должно работать!

---

## Если ничего не помогает

1. Прочитайте `SETUP_GUIDE_RU.md` полностью
2. Проверьте логи ошибок:
   ```bash
   docker-compose logs
   npm run dev
   ```
3. Убедитесь, что следуете инструкциям ПО ПОРЯДКУ из `START_HERE.md`

---

**Большинство проблем решаются выполнением:**
```bash
npm install
npm run build
docker-compose up -d
```

