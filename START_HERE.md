# 🎯 СТАРТОВАЯ ТОЧКА - Что делать сначала

## ⚠️ ВАЖНО: Сначала прочитайте это!

Если у вас возникают ошибки или вы не знаете, с чего начать - следуйте этому руководству ПО ПОРЯДКУ.

## Шаг 1: Проверка установленных программ

Убедитесь, что у вас установлено:

- ✅ **Node.js 20+** - https://nodejs.org/
- ✅ **Docker Desktop** - https://www.docker.com/products/docker-desktop/

Проверьте установку:
```bash
node --version    # должна быть v20.x.x или выше
npm --version     # должна быть 10.x.x или выше
docker --version  # должна быть любая версия
```

## Шаг 2: Создайте файл .env

**Файл .env обязателен!**

1. Откройте папку `C:\Konkurs` в проводнике Windows
2. Создайте новый текстовый файл
3. Назовите его `.env` (с точкой в начале!)
4. Откройте файл и вставьте:

```
NODE_ENV=development
PORT=3000
MONGODB_URI=mongodb://admin:password123@localhost:27017/auctions?authSource=admin&replicaSet=rs0
REDIS_URL=redis://localhost:6379
JWT_SECRET=your-secret-key-change-in-production-change-this-in-real-app
```

5. Сохраните файл

**Подробнее:** См. файл `CREATE_ENV.md`

## Шаг 3: Установите зависимости

Откройте терминал в папке `C:\Konkurs`:

```bash
npm install
```

⚠️ **Это займёт 2-5 минут!** Дождитесь завершения.

Если видите ошибки "Cannot find module" - это нормально на этом этапе. После установки ошибки исчезнут.

**Подробнее:** См. файл `FIX_ERRORS.md`

## Шаг 4: Установите зависимости frontend

```bash
cd frontend
npm install
cd ..
```

## Шаг 5: Запустите Docker контейнеры

**ВАЖНО:** Docker Desktop должен быть запущен!

```bash
docker-compose up -d mongodb redis mongo-setup
```

Подождите 15-20 секунд после запуска.

Проверьте статус:
```bash
docker-compose ps
```

Должны быть контейнеры в статусе "Up".

## Шаг 6: Соберите проект

```bash
npm run build
```

Если видите ошибки - см. `FIX_ERRORS.md`

## Шаг 7: Создайте тестовые данные

```bash
npm run create-test-data
```

**⚠️ ЗАПОМНИТЕ ID активного аукциона из вывода!** Он понадобится для тестирования.

## Шаг 8: Запустите сервер

```bash
npm run dev
```

Вы должны увидеть:
```
✅ MongoDB connected successfully
✅ Redis connected
🚀 Server running on port 3000
```

Если видите ошибки - см. `SETUP_GUIDE_RU.md`, раздел "Решение проблем"

## Шаг 9: Запустите frontend

**Откройте НОВЫЙ терминал** (не закрывая backend):

```bash
cd C:\Konkurs\frontend
npm run dev
```

## Шаг 10: Откройте в браузере

Откройте: **http://localhost:5173**

## ✅ Всё работает?

Отлично! Теперь вы можете:
- Создавать аукционы
- Делать ставки
- Наблюдать за завершением раундов
- Тестировать систему

## ❌ Что-то не работает?

### Ошибки импорта TypeScript?

→ См. `FIX_ERRORS.md`

### MongoDB не подключается?

→ См. `SETUP_GUIDE_RU.md`, раздел "Решение проблем"

### Не знаете, что делать?

→ См. `SETUP_GUIDE_RU.md` - полное подробное руководство

## Полезные файлы документации

- **START_HERE.md** (этот файл) - начало работы
- **SETUP_GUIDE_RU.md** - полное руководство по установке
- **CREATE_ENV.md** - как создать файл .env
- **FIX_ERRORS.md** - как исправить ошибки
- **QUICKSTART.md** - быстрый старт для опытных
- **README.md** - полное описание системы

---

**Удачи! Если возникнут проблемы - читайте документацию по порядку!** 🚀

